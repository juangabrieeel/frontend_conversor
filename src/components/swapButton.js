import React from "react";

// Botón para realizar el intercambio de las unidades métricas
const SwapButton = ({ onClick }) => {
  return <button onClick={onClick}>Intercambiar</button>;
};

export default SwapButton;
